# Sample docker-compose file il alsa mode

## Usage

Copy the provided `sample.env` to `.env`:

```text
cp sample.env .env
```

Edit with your favorite editor, the run with:

```text
docker-compose up -d
```